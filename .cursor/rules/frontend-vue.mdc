---
description: Vue 前端工程约定
globs: frontend/**
alwaysApply: false
---

# Frontend（Vue）

- 工程根目录仅为 `frontend/`，与后端解耦；通过环境变量配置 API 基地址。
- 使用 Vue 3 + Composition API；禁止引入过时 Options API 作为新代码默认写法（存量除外）。
- 按业务域拆分模块（会员、门禁、商户、课程/预约等），避免巨型单页堆砌。
- 管理端与（如有）Pad/门禁相关页面分离路由与布局；Pad 界面以通行反馈为主，少交互。
- API 调用统一封装；错误与鉴权（Token 失效）集中处理。
- 样式与组件遵循现有工程约定；注释使用中文。
- 不在前端存储人脸原始特征或敏感生物数据；仅展示业务状态与必要脱敏信息。

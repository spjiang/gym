---
description: 健身房管理系统整体架构、目录与技术栈约定
alwaysApply: true
---

# 项目架构

回龙观公园综合经营场地（健身房、酒吧、招商商户）的管理系统。门禁使用人脸 Pad。设计时预留多业态、多商户扩展，避免写死为单一健身房。

## 技术栈

| 层级 | 选型 |
|------|------|
| 前端 | Vue（独立工程 `frontend/`） |
| 后端 | Python（独立工程 `backend/`） |
| 数据库 | PostgreSQL |
| 部署 | Docker Compose |
| 消息队列 | 需要异步/解耦时用 RabbitMQ（门禁事件、通知、同步等） |

## 目录

```
项目根目录/
├── frontend/          # Vue 前端（独立工程）
├── backend/           # Python 后端（独立工程）
├── docs/              # 项目文档
├── docker-compose.yml # 统一编排（api / web / postgres / rabbitmq 等）
├── .gitignore
└── README.md
```

- 前后端分离：各自独立依赖、构建与版本管理，仅通过 HTTP/WebSocket API 协作。
- 基础设施（Postgres、RabbitMQ）由根目录 Compose 统一编排，勿在子工程内再起一套生产依赖。
- 业务说明与接口约定优先落在 `docs/`，代码注释使用中文。

## 领域边界（兼容多业态）

- **场地/租户**：园区或经营主体；后续可挂多个商户。
- **商户**：健身房、酒吧、招商入驻店等，权限与资源按商户隔离。
- **会员/通行**：会员可跨业态；人脸门禁以「通行主体 + 授权范围（门禁点/时段/商户）」建模，勿绑死单一门店类型。
- **设备**：人脸 Pad 作为门禁终端，通过设备注册、心跳、通行事件上报与后端交互；耗时同步走 RabbitMQ。

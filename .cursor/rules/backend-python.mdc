---
description: Python 后端工程约定
globs: backend/**
alwaysApply: false
---

# Backend（Python）

- 工程根目录仅为 `backend/`；对外提供 REST（或必要 WebSocket）API，供 `frontend/` 与门禁设备调用。
- 数据库使用 PostgreSQL；通过迁移管理 schema，禁止靠手工改库作为常规流程。
- 多商户场景下，查询与写入必须带商户/场地范围条件，防止越权。
- 门禁相关：
  - 设备鉴权与业务用户鉴权分离。
  - 通行校验接口需低延迟、可幂等；人脸比对结果以设备侧或既定服务为准，后端负责授权与审计。
  - 通行日志、通知、批量同步等异步任务投递 RabbitMQ，避免阻塞开门路径。
- 仅在确实需要解耦/削峰/异步时引入 RabbitMQ；同步 CRUD 不要强行走队列。
- 配置经环境变量注入；密钥、数据库连接串不得提交仓库。
- 注释与文档字符串使用中文；不使用已过时或弃用的库 API。

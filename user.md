目录级 Demo 已写入（`SEED_DEMO=true`，可反复执行、不重复造数）。登录页可按组织角色一键切换身份。

**可直接用的账号**

| 层级 | 用途 | 账号 | 密码 |
|------|------|------|------|
| 场地 | 场地管理员 | `admin` | `Admin@123456` |
| 场地 | 场地运营 | `site_ops` | `Demo@123456` |
| 健身房商户 | 管理员 | `gym_admin` | `Demo@123456` |
| 健身房商户 | 运营 | `gym_ops`（兼容 `front01`） | `Demo@123456` |
| 健身房商户 | 教练 | `coach01` | `Demo@123456` |
| 清吧商户 | 管理人员 | `bar_admin` | `Demo@123456` |
| 清吧商户 | 运营 | `bar_ops` | `Demo@123456` |
| 清吧商户 | 收银 | `bar_cashier` | `Demo@123456` |
| 会员 H5 | 登录 | `13800001001` | 验证码 `123456` |

角色采用 **A+B**：场地维护 `tpl_*` 标准包；商户挂接业态后复制为本商户实例，商户管理员可微调本商户角色与员工。

**配置台（场地管理员）**：综合经营 →「子系统配置」「角色权限」「支付配置」「支付对账」。

**退款**：管理端订单退款（会籍/课包按剩余价值建议额，退后终止权益；零售/餐饮可部分退；线上原路、线下选手动渠道）。会员餐饮不提供自助退，请联系前台。

**会员 H5**：登录后先选店；健身房走会籍/团课等；清吧可点餐、支付见取餐号、申请退款。账号 `13800001001` / 验证码 `123456`。微信内走 JSAPI、站外走 MWEB；`DRY_RUN` 下预下单后自动确认履约。

**小程序**：打开 `miniprogram/`，登录后绑定 openid，选店 → 商城可购卡/课包支付（见 `miniprogram/README.md`）。

**获客码**：管理端「商户组织」→ 行操作「获客码」；扫码打开 `/login?merchant_id=`，新用户 OTP 自动注册并挂靠，主档记录首次来源（商户或「综合运营平台」）。

关闭 Demo：`.env` 设 `SEED_DEMO=false`。
